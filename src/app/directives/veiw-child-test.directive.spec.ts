import { VeiwChildTestDirective } from './veiw-child-test.directive';

describe('VeiwChildTestDirective', () => {
  it('should create an instance', () => {
    const directive = new VeiwChildTestDirective();
    expect(directive).toBeTruthy();
  });
});
